# Stadium Aura EQ Engine

Implemented now: a real smoothed compact tilt/tone stage whose displayed curve can be derived directly from the active `tone` parameter.

TODO: 8–12 APVTS bands, expanded editor, multiple filter types, filter slopes, per-band dynamic EQ, and eventual 24-band architecture. No spectrum is drawn until a real prepared lock-free analyzer is implemented.
